dubbo:
  application:
    name: forking-provider
  protocol:
    port: 30883
    name: dubbo
  registry:
    address: zookeeper://127.0.0.1:2181
  scan:
    base-packages: com.study.dubbo.failover.forking

logging:
  level:
    com.study: debug